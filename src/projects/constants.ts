/** Path of the root folder */
export const ROOT_PATH = "/"

/** Name of the dependencies folder */
export const DEPENDENCIES_FOLDER_NAME = "dependencies"

/** Path of the dependencies folder */
export const DEPENDENCIES_FOLDER_PATH = ROOT_PATH + DEPENDENCIES_FOLDER_NAME

/** Name of the package.json file */
export const PACKAGE_JSON_NAME = "package.json"

/** Path of the package.json file */
export const PACKAGE_JSON_PATH = ROOT_PATH + PACKAGE_JSON_NAME

/** The name of the hyperapp project. */
export const HYPERAPP_NAME = "hyperapp"

/** The collection that holds the projects. */
export const COLLECTION = "projects"

/** The ID for non-saved projects. */
export const NEW_PROJECT_ID = "new"
