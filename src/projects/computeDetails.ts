// import { PackageJson, inferMainFile } from "lib/npm"
// import { guid } from "lib/utils"

// import { Details } from "./api"
// import { getSearches } from "lib/search"

// export function computeDetails(pkg: PackageJson, existing: Details): Details {
//   const result: Details = {
//     id: existing.id,
//     name: pkg.name,
//     mainFile: "/" + inferMainFile(pkg),
//     searches: getSearches(pkg.name),
//     hidden: false
//   }

//   return result
// }
